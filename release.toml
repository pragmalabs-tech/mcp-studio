tag-name = "v{{version}}"

# Commit message when bumping versions
pre-release-commit-message = "release: v{{version}}"

# Push tag and commits to remote
push = true
tag = true
publish = false  # CI handles artifact publish after building from the tag
